.venv/
models/
cache/
__pycache__/
*.pyc
# .env.example says "copy to .env for local runs", and that copy holds
# TTS_API_KEY. Without this line the provider credential is one `git add .`
# away from being published.
.env
